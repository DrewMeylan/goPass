02-15-25
 >> In utils, combine encryption functionality with the writeFile function. These do not need to be separate function calls.
 >> Similarly, create a ReadFunction which encompasses the decryption and re-encryption of the read password
 >> GPG keys / recipients should be defined on a per-store basis; add functionality at initialization to define gpg keys
 >> add flags to the create local* and create cluster calls to allow specification of which store to update
   >> Local is supposing we decide to allow multiple distinct local stores versus one shared store with subdirs
 >> Add functionality to store intitalization to instantiate the local directory as a git repository
